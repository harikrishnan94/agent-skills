{
  "//": "Merge into ~/.cursor/hooks.json to enable hooks/cursor/session-scratchpad.sh. Cursor loads user-level hooks from ~/.cursor/hooks.json; `command` should be an absolute path to the installed hook (symlinked into ~/.cursor/hooks/). CAVEAT: the Cursor CLI (cursor-agent) currently fires only before/afterShellExecution — sessionStart fires in the Cursor IDE, so this hook is effectively IDE-only until the CLI gains sessionStart support.",
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "~/.cursor/hooks/session-scratchpad.sh"
      }
    ]
  }
}
